# Production — nginx proxies /api → backend container
# If your backend is on a different domain, set it here:
# VITE_API_URL=https://api.yourdomain.com/api
#
# If using same-host nginx proxy (default docker-compose setup),
# leave this blank and nginx will forward /api → backend:5000
VITE_API_URL=
